# Pipeline output (per-RFE artifacts)
.artifacts/

# Parked ideas (local planning, not tracked)
future.md
tmp/
reports/

# Decision artifacts (generated by decision-kit, not source)
.decisions/

# Fetched context (downloaded at runtime)
.context/

# Credentials
.env/

# Node (trailing slash: ignores real node_modules dirs but NOT the root symlink,
# which is committed for ESM module resolution — see eval README)
node_modules/
package-lock.json

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.ruff_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Test fixtures and eval harness (development-only, not shipped)
.claude/skills/eval/tests/fixtures/
.claude/skills/eval/evals/
.claude/skills/eval/eval-harness/
.claude/skills/eval/eval-optimize-mlflow/

# Migration guide (transitional doc for UXD AI Helpers integration, not pushed)
.claude/skills/eval/MIGRATION.md

# Stray Playwright scripts from eval runs (should be in .artifacts/)
.claude/skills/eval/journey-test-*.mjs
.claude/skills/eval/persona-walkthrough-*.mjs

# Run metrics (generated per-run, not source)
.artifacts/**/run-metrics.json

# MLflow (development — cluster is canonical; see deploy/openshift/)
mlflow.db*
*.db.bak*
*.db.old*
mlruns/
.claude/skills/eval/test-results/
.claude/skills/eval/scripts/mlflow-compare-models.py
.claude/skills/eval/scripts/mlflow-trace-eval.py
.claude/skills/eval/scripts/mlflow-trace-pipeline.py

# Eval harness run outputs (created by /eval-run)
eval/runs/

# Claude settings (personal env vars, cluster URIs)
.claude/settings.json

# OpenShift deploy manifests (development, cluster-specific)
deploy/

# Ad-hoc scripts and planning docs (local-only, not pipeline code)
.claude/skills/eval/PLAN-*.md
.claude/skills/eval/scripts/query-model-costs.py
.claude/skills/eval/scripts/judge_report_interactive.py

# Root scripts/ ad-hoc dev scripts (not pipeline code)
scripts/__init__.py
scripts/__pycache__/
scripts/generate-report-fixtures.py
scripts/run-persona-walkthrough.sh
scripts/test-report-judges.py
scripts/run-eval-extract.py
scripts/judge_report_interactive.py

# Project state snapshots (local working docs, not tracked)
project_state.md
mlflow-harness-state.md
mlflow-audit-roadmap.md
mlflow-openshift-runbook.md
mlflow-run-cheatsheet.md

# Cursor workspace state
.cursor/

# Presentation drafts and personal notes
docs/drafts/

# OS
.DS_Store
Thumbs.db